road.cedge:
> n x (edge index, snode, enode, weight(integer))
USED BY: gtree_build
GEN BY: trans.py

_road.nedge:
> n x (snode, enode, weight(interger))
USED BY: trans.py

road.nedge:
> (node num, edge num)
> n x (snode, enode, weight(interger))
USED BY: GPTree_build

road.cnode:
> n x (node index, longitude, latitude)
USED BY: gtree_build gtree_query
